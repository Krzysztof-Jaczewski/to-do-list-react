import styled from "styled-components";

export const StyledMain = styled.main`
  display: grid;
  grid-template-rows: auto 1fr;
  grid-gap: 10px;
  max-width: 900px;
  margin: 0 auto;
  padding: 0 20px;
`;
